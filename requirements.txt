--extra-index-url https://download.pytorch.org/whl/cpu
fastapi==0.115.0
uvicorn[standard]==0.32.0
transformers==5.13.1
# transformers 5.13 requires PyTorch 2.4 or newer. This installs a CPU-only
# wheel on Render (which does not provide a GPU on the free plan).
torch==2.5.1
jinja2==3.1.4
pydantic==2.9.2
python-multipart==0.0.12
